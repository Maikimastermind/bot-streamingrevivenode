version: '3.8'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bot-streaming-node
    ports:
      - "3000:3000" # Ajusta si usas otro puerto
    restart: unless-stopped
